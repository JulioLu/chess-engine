#include "../inc/pawn.hpp"


Pawn::Pawn(uint8_t pieceID, char color, uint8_t coordinateX, uint8_t coordinateY):Piece(pieceID, color, coordinateX, coordinateY){
    //  this->pieceID = pieceID;
    // posision[0] = posisionX;
    // if(color=='w')
        // posision[1] = 1;
    // else
        // posision[1] = 6;
}
